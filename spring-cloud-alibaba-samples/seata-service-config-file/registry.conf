registry {
  # 这里是与yml有关的相关配置【配置yml】，配yml时要看这里的数据。【一定要对应上】
  # file 、nacos 、eureka、redis、zk、consul、etcd3、sofa
  type = "nacos"

  nacos {
    application = "seata-server"
    serverAddr = "127.0.0.1:8848"
    group = "SEATA_GROUP"
    namespace = "SEATA_GROUP"
    cluster = "default"
    username = "nacos"
    password = "nacos"
  }
 
}

config {
  # file、nacos 、apollo、zk、consul、etcd3
  type = "nacos"

  nacos {
    serverAddr = "127.0.0.1:8848"
    namespace = "SEATA_GROUP"
    group = "SEATA_GROUP"
    username = "nacos"
    password = "nacos"
  }
 
}
